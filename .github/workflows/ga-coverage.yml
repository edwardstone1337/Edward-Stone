name: GA Coverage

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  ga-coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check GA coverage
        run: ./scripts/check-ga-coverage.sh
