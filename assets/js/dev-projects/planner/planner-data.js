/**
 * Planner prototype — mock data.
 *
 * Placeholder only. The entity model (units, lessons, weeks) and its
 * shape will be defined once the brief lands; this module exists so
 * the shell has a data module to import from without inventing a
 * schema ahead of time.
 */

export const plannerData = {};
