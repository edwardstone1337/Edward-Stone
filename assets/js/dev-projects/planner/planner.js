/**
 * Planner prototype — entry point.
 *
 * Shell only. This module will grow into the working prototype's
 * interaction logic (scheduling, drag/drop, view state, etc.) once
 * the brief lands. `initPlanner()` is a deliberate no-op for now —
 * it exists so the gated shell page (projects/planner.html) has a
 * real module to import and call rather than an inline placeholder.
 */

export function initPlanner() {
  // Intentionally empty — prototype logic lands here per the brief.
}
